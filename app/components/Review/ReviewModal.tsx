import { Button, Card, CardBody, Image } from "@nextui-org/react";

export default async function ReviewModal({
    searchParams,
}: {
    searchParams: { gameID: string };
}) {
    

    return (
        <div className="flex flex-col pl-10 pt-10 w-full">
        </div>
    )
}